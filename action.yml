name: 'Auto Release Draft'
description: 'Drafts a GitHub Release with changes introduced by newly created version tag.'
author: 'Enrico Campidoglio'
outputs:
  release-url:
    description: 'URL of GitHub Release that was drafted'
runs:
  using: 'node16'
  main: 'dist/index.js'
